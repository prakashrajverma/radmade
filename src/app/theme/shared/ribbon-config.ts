export class RibbonConfig {
  type?: string; // ribbon-bookmark, ribbon-corner
  icon?: string; // fa-home
  class?: string; // ribbon-default, ribbon-primary, ribbon-info, ribbon-danger, ribbon-warning, ribbon-success
  position?: string; // ribbon-right, ribbon-vertical-l, ribbon-vertical-r
}
